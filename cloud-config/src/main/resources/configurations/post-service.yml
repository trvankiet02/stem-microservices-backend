eureka:
  instance:
    hostname: localhost
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/

server:
  port: 8700

spring:
  application:
    name: post-service
  data:
    mongodb:
      database: post_service
      uri: mongodb+srv://admin:123@springbootwithmongodb.xffyyfw.mongodb.net/

management:
  tracing:
    sampling:
      probability: 1.0