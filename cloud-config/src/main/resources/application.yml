server:
  port: 8888

spring:
  profiles:
    active: default
  application:
    name: cloud-config
  cloud:
    config:
      server:
        git:
          uri: https://github.com/trvankiet02/cloud-config-server.git
          basedir: /tmp/cloud-config-repo

